# The file is the thought. Prices stay empty.

i write the day like a recipe because the other way i start lying. the number can wait. the file cannot. unbound is a kind of prayer i trust.

## When

You have a `.method` / cooklang file. Operations as a recipe.

## What

`@resource` `#infra` `~timer` and a metadata invariant. The file is the AST. Markdown is derived.

## How

Compile. Do not invent prices. Cost is never an input. Do not wrap the file as a plug.

## Use when

Someone says “just put a dollar on it.” Someone wants the note to be the source.

Late bind. Blank is valid.
